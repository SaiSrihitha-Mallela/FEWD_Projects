import React from 'react';

const HeroSection = () => {
    return (
        <section>
            <h1>Welcome to the Homepage!</h1>
            <p>This is a simple React component structure.</p>
        </section>
    );
};

export default HeroSection;
